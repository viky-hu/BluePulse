This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

# Getting Started
pnpm dev
http://localhost:3000

oying) for more details.

# 前情提要

1、本系统使用Next.js框架，使用TypeScript编写，使用TailwindCSS进行样式设计，使用GSAP进行动画效果。
2、本系统惯用英文字体为Google Sans，下载方式在[text](GoogleSans字体下载.txt)
3、本系统惯用中文字体为微软雅黑
4、本系统中出现的许多标题大字都是独立的SVG字体而非单纯的文本框加字，默认不使用粗体字。前者的目的是便于实现类似C:\Users\Admin\csy\apps\main-platform\app\windows\login所实现的大字独立SVG可被色块所覆盖同步换色的巧妙设计，这里你可以深度阅读学习一下具体实践，将这种设计思想照搬过来写入README.md文档中。我们今后一定会用到。
5、本系统的文件夹整理将符合vercel给出的react最佳实践，谨防出现同一个tsx或css文件中堆积很多页面样式的方法，我认为这不符合最佳实践 也不利于定位查找某个组件的代码
6、由于我不是专业前端工程师，所以我习惯在设计电脑版网页的时候，用小尺大致测量我希望的组件长宽尺寸，在这里 我告诉你我的笔记本打开网页全屏后长34cm 宽19cm，这一点你要记住
7、按“桌面保留 34×19 的测量基准、移动端切换为短边约束的 2:3 响应式比例”记录，避免把物理 cm 单位硬编码进浏览器
# 初始页面

本系统将极力使用GSAP驱动动画效果，学习和利用GSAP官方的优秀实践和官方Skills，力求做出最优秀的动画效果，谨防闭门造车，自造轮子，避免重复造轮子。
前端工作者对本系统的优化意见发送给AI，AI必须深度思考已有实践并阅读相关文档、Skills和已有相关代码，积极联网搜索，必要地，需要询问前端工作者相关技术细节，确保优化方案的可行性和先进性。

## 初始页面的 GSAP + SVG + 时间轴

### 初始页面最开始的黑色画布和线条动效

初始状态下，由一幅#353330的纯色SVG背景覆盖屏幕，（屏幕内原来的颜色是#F2F2F2的偏白色，从打开网页的开始就一直被#353330的SVG大画布覆盖，不会出现渲染没跟上 露出#F2F2F2白色背景的情况）之后开始绘制一个线条logo，此logo的绘制运用了GSAP的drawSVG插件，遵循了GSAP官方的优秀实践和官方Skills。

事后也在根目录制作了GSAP闭合SVG描边动画方法论，供后续学习利用。

### 线条绘制结束后：

本次优化点：
1、logo的淡出应当加快，其余不变，只增快淡出速度

2、参考split-text动效.txt，在logo淡出后，紧跟着，立刻播放split-text动效的英文词组：“Blue Pulse”，字体为Google Sans，可以再来一行小字注明“国内外警务科技情报系统”

3、对于第2点，要求加快split-text动画速度 使得字体完全通过动画呈现出来的速度增快，一上来可以做大字号 如果太大了我们再后续调整，字色#D5AF86，与logo线条的颜色一样。浮于最上层，不被遮挡但可能会联动SVG背景移动。

4、假设整个网页打开后全屏长34cm 宽19cm，我们设计一个矩形SVG画布，左右宽度大约在10cm，上下长度（高度）大约在15cm，这个矩形SVG画布的中心点位于全屏的中心点，这样可以保证整个画布在全屏中居中显示，这个矩形SVG色块的层级与颜色并非固定，非常复杂，请听我详细说明：

（1）它可能大致处于全屏#353330大画布背景之上，但是比较复杂，我拿不准，但是一定在logo线条和大字的层级之下。

（2）它自身拥有常驻的阴影，所以看上去就有浮于上方的效果

（3）它的颜色一开始看上去就是#353330颜色 和大画布颜色一样，但是因为有阴影所以很显然能看出来。会根据此时覆盖的全屏大画布背景的状态所决定，后续全屏黑色画布将会向上滑动收起，在这个过程中下边界向上抬升，必然会通过这个矩形画布，对于背景，会露出原本的#F2F2F2背景，而对于这个矩形的颜色，此时我们会发现它露出的颜色是#FDFDFD的偏白色，最后的效果是黑色画布向上抬升直到退出屏幕，露出原本的#F2F2F2背景和#FDFDFD的偏白色带阴影矩形画布

（4）这个矩形画布如果能够做成 我希望它是一个独立可复用的组件，足够成熟的话，以后我也用的上

5、第4点中主要提及矩形画布，但还没完全说明从头到尾的动效时间轴的细节：

黑色全屏画布，初始状态下什么都没有

然后开始绘制线条logo，绘制完直到它淡出后同步立刻开始split-text两行一大一小的字

在浮现这两行字的过程中，同步淡入出现这个矩形画布，也就是说这个矩形画布并非一开始就存在，而是也是淡入出来的，具体是淡入显现时尺寸略小一点 然后像收缩后变大一样尺寸微微扩张到我要求的尺寸。

同步地 淡入浮现一个按钮位于下方居中，说明在D:\pulse\button动效1.txt，此按钮位置应该在此矩形下边界上方一些距离，可按下 按下/向下滚动鼠标滚轮后则触发以下动效————

按钮按下/向下滚动鼠标滚轮后，按钮淡出消失，封面的大字也淡出消失

同步地 全屏黑色画布下边界开始向上收起，视觉上带来黑色画布向上退出页面的效果，露出原本的#F2F2F2背景和#FDFDFD的偏白色带阴影矩形画布

自此初始页面动画结束

#### 第二次优化提示词
1、将BluePulse字体和下方文字改成粗体
2、你复用了D:\pulse\button动效1.txt，这很好，但是在视觉上显得粗糙，我希望你加粗圆圈、做大做粗内部箭头；另外，请将箭头在视觉上的边界增大，因为我发现当我在hover的时候根据箭头动画是确实是会向下快速滚动，但是由于你设定的视觉边界过窄，导致我看不到完全的向下滚动的箭头，而是看上去像是被割掉了下边界一样
3、对于我们已经实现的这个矩形画布组件，在出现的动效、具体视觉上的问题还有些我需要向你反馈：
（1）当处于初始状态黑色全屏画布存在的情况下 我发现这个矩形画布虽然颜色确实是黑的，但是却存在一条极其细微的白色细线在边界，视觉上较为突兀
（2）本矩形画布的出现，或是说淡入的过程，我希望是这样的动态效果：重心中心点不变的情况下，初始淡入显现的时候尺寸没有这么大，它的长宽会略小一点，然后迅速地扩张到我要求的尺寸
（3）我希望文字的出现和这个矩形的淡入在时间轴上的排布不是同时的 而是错开来，我希望略微向后延迟一点矩形画布淡入的时刻

### 黑色画布全部退出后

补充任务：现在黑色全屏画布的向上移动过程不够柔和优雅，我建议用慢快慢的缓动效果来实现 请你搜索相关实践以实现

进入正题：黑色画布完全退出后，整个页面将呈现#F2F2F2背景和#FDFDFD的偏白色带阴影矩形画布

#### 做一些必要工作（已完成）

1、撰写gitignore文件
2、将字体的安装一并纳入git中，方便他人从github上拉取并在其他电脑上运行

#### 第二阶段具体动效
在 Next.js 16.3.5、React 19、TypeScript、Tailwind CSS 4 和 GSAP 3.15 基础上，构建动效。

执行前必须阅读本地 Next 16 文档及 `gsap-core`、`gsap-react`、`gsap-timeline`、`gsap-plugins`、`gsap-performance` Skills。实现重心放在视觉与动效，不开展冗长验收。

以下是第二阶段的动效制作

具体切入点：按下那个圆形按钮后，然后黑色画布开始向上收起，大致收起到页面高度的一半后延迟一点点时间，开始触发以下动效————

1、使用#A4D7C7的浅绿色，制作一个尺寸外形动态gsap变化的svg组件，层级位于矩形画布模块之下，位于白色背景布之上
2、这个组件具体是这样动态变化的：当黑色画布大致收起到页面高度的一半时，延迟启动，在整个页面的横中轴线位置开始，同时向上下两侧延伸并铺开#A4D7C7的画布，速度应该是慢快慢的递进变化，主要是快速铺开的gsap动效
3、具体说一下其尺寸中的变与不变：#A4D7C7扩张画布组件并不会一直覆盖到上边界，而是距离上边界2cm左右（详见README前情提要6、7）但是会一直到下边界。不变的是其左右宽度，因为其是从中轴线位置开始向上和向下扩张的，所以从始至终从左到右的尺寸都是不变的，但并不是一直到屏幕左右边界，而是大致距离左右边界0.5cm左右

待到这个浅绿色画布完全展开稳定后，在gsap的时间轴上，在此略微停顿1秒，然后开始执行以下动效————

注：接下来的组件 无论是大字还是线都是独立的可随动态渲染变化的SVG组件，仍然需要参考GSAP相关优秀实践，并选用成熟动效组件和选取合适参数：

仍然将横中轴线作为基准，从 距离左边界1cm处开始使用一条不细线状组件（不会特别细），以慢快慢的速度向右延伸（总绘制线程很快），一直画到距离右边界1cm处，此时这根中轴上的黑色横线一直存在，并接下来将持续1秒存在，之后，将使用gsap动画的逆过程，让线条从左开始向右以同样的速率参数收起，直到从右侧1cm处整根线完全消失。

以上这段时间轴并非只承载线条的绘制 而是伴随以下新的大字组件，但是此组件的视觉效果更加复杂：

何时触发？是在这跟黑色线条很快向右延伸绘制到距离右边界1cm处的这个终点这一刻，同步触发

大字内容：“Latest Report”，位于最上层

其显现的方式极其巧妙且难以实现，请听我娓娓道来：

不是简单的“文字淡入”，而是一个很典型的 “中轴线切开文字 + 上下分离展开” 的转场效果。不只是“文字从黑线附近出现”，而要明确成：

文字初始被压缩/裁切在黑色水平中轴线附近，随后以中轴线为分界，上半部分向上展开、下半部分向下展开，逐渐恢复完整字形

之后 文字整体将持续存在，只是线条之后会收起消失

文字完全显现后，下方一定距离可以再淡入一行字“基于大模型智能分析推送最新资讯”，使用略灰一些的黑色字

都用粗体字